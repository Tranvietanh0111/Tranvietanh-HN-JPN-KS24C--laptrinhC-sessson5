#include <stdio.h>

int main() {
    int dapan = 25; 
    int doan; 
    while (dapan != doan) {
        printf("Nhap 1 so: ");
        scanf("%d", &doan);

        if (dapan == doan) {
            printf("ban da nhap dung \n");
           
        } else {
            printf("Sai roi\n");
            }
    }
    
return 0 ;
}

